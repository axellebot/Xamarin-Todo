<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          x:Class="XamarinTodo.Views.TodoItemCell"
          xmlns:messier16="clr-namespace:Messier16.Forms.Controls;assembly=Messier16.Forms.Controls">
    <ViewCell.ContextActions>
        <MenuItem Command="{Binding DeleteItemCommand}" Text="Delete" IsDestructive="True" />
    </ViewCell.ContextActions>
    <StackLayout Orientation="Horizontal" Padding="10">
        <messier16:Checkbox Checked="{Binding Done,Mode=TwoWay}" WidthRequest="45" VerticalOptions="CenterAndExpand"/>
        <StackLayout Orientation="Vertical">
            <Label Text="{Binding Name}" 
                                   LineBreakMode="NoWrap" 
                                   Style="{DynamicResource ListItemTextStyle}" 
                                   FontSize="16" />
            <Label Text="{Binding Notes}" 
                                   LineBreakMode="TailTruncation"
                                   Style="{DynamicResource ListItemTextStyle}" 
                                   FontSize="12" HeightRequest="20" />
        </StackLayout>
    </StackLayout>
</ViewCell>
